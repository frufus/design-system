node_modules/
dist/
storybook-static/
coverage/
test-results/
playwright-report/
*.local
.DS_Store
.vite/

# The seeded canvas is a 2 MB build artifact of docs/design/*.dc.html plus
# canvas.json. Those are the sources; this is regenerated by the design skill.
docs/design/frufus-design-system.html

# OpenSpec's archive lock is a runtime claim, not content. Committing it means
# a stale lock travels to every clone.
openspec/changes/archive/.openspec-archive.lock

# openspec archive leaves its claim behind; a committed lock blocks the next
# archive on every other machine.
openspec/changes/archive/.openspec-archive.lock
